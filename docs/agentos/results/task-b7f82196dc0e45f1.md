# Результат task-b7f82196dc0e45f1

Статус: CLOSED (локальный этап).

Public AgentOS 0.5.0-beta.4 was published from f69a74620a614c2f55c17ceacfe4a9c18dfc0455 with optional external profiles. The release wheel was independently downloaded and matched its published SHA-256. All four stage checks passed.

## Ограничения

The public release does not activate the Mac profile or replace the private runtime. The live Mac installation is pending a separate host gate. Same-UID races and heuristic secret detection remain documented limits; Windows and website target proofs are separate.

## Следующий этап

Review the external Mac profile candidate with the owner; install the public release side by side only after its host gate can be satisfied, then prove native hook activation. Preserve the existing private runtime until that target proof.
