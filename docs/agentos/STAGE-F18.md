# Stage F18 — public foundation independence

Task: `task-593c921d94644bf6`

Status: VERIFIED SOURCE AND MAC CORRECTION; 2026-09-26.

## Objective and evidence

An inspected NEW application chat treated private fleet admission/controller
requirements as the general AgentOS route. Its target status remains evidence
from that chat, not a fresh host check. Local shared skills also assume private
notification/runtime policy applies to every AgentOS task.

## Architecture and scope

Public project governance runs using the public package and local project docs.
Private runtime, deployment controllers and notification providers are optional,
separately selected components. Existing enforced target controls remain in
force; inherited advice alone does not prove such enforcement or create it.
Document the distinction and a bounded diagnosis route. Add an English public
guide and links in architecture, process, overlay and entry guidance. Repair
only backed-up Mac applicability fragments in global instructions and the local
orchestration/reporting skills. Record exact private evidence outside core.

## Acceptance and checks

No private installation, RC pin, fleet signer/profile or notification provider
is a prerequisite for public development or local checks. An actual target
refusal still blocks that effect and identifies its source and owner. Missing
optional reporting never blocks task completion. Existing permitted direct
execution requires its own authority and rollback; refusal has no bypass.
Run registered full-tests and resource-parity, inspect the complete diff and
read back exact Mac changes. Local tests do not prove target policy migration
or agent behavior in a new chat.

## Delivery, rollback and limits

Direct reviewed Git delivery to the canonical public repository; immutable
0.5.4 tag/assets and installed core remain unchanged. New packaged guidance
ships with the next separately verified release. Exact Mac backups allow
fragment rollback without replacing unrelated owner text. No NEW source,
MG/service/RU runtime, loader, ACL, credentials, notifications, hooks or client
security changes. Actual target-policy migration is a separate authorized task.

## Verified result and limitations

278 existing tests pass under installed foundation 0.5.4; receipt
`8218e62bf9824dd59393734bd7d00d9b`. Source/resource parity and complete diff
review passed. No blocking findings; no executable core behavior or machine
schema changed. This is documented guidance, not a new enforcement feature.
Five exact local instruction/skill preimages were saved before scoped edits;
all five after-hashes matched read-back. The owner adapter update is visible
in the active instruction context. The private knowledge pointer resolves and
native hooks remain absent. Actual remote controls and the NEW candidate were
not modified or freshly tested; no notification or message to the inspected
chat was sent. Installed core and immutable published 0.5.4 remain unchanged;
new public guidance is source-only until the next verified release.
