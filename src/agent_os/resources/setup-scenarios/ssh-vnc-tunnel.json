{
  "schema": "agentos.setup-scenario/v1",
  "id": "ssh-vnc-tunnel",
  "version": "1.1.1",
  "title": "Connect with SSH and verify optional VNC transport",
  "goal": "Reach one authorized device quickly through a verified SSH route and optionally verify protected RFB transport. Desktop viewer integration is deferred.",
  "implementation_status": "guide_only",
  "evidence": {
    "level": "live_verified",
    "scope": "One authorized macOS host passed authenticated SSH and RFB transport through the bounded diagnostic on 2026-09-26. VNC login and a desktop frame were not verified. Direct and reverse setup guidance remains documented, not universally tested. Screen Sharing and other desktop viewer integration are deferred from the active scenario.",
    "reviewed_on": "2026-09-26",
    "sources": [
      "https://man.openbsd.org/ssh",
      "https://man.openbsd.org/ssh_config",
      "https://man.openbsd.org/sshd_config",
      "https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh_install_firstuse",
      "https://support.apple.com/guide/mac-help/mchlp1066/mac",
      "https://www.rfc-editor.org/info/rfc6143/"
    ]
  },
  "prerequisites": [
    "Know the exact owner-authorized target device and whether SSH only or optional VNC transport verification is wanted. Desktop viewer integration is deferred.",
    "Check the selected user's existing connection records and SSH aliases first; treat an old route as a candidate until current identity and access are verified.",
    "Use a verified operator identity and host key. Keep private keys, passwords, device addresses and per-user routes outside the public card."
  ],
  "flows": [
    {
      "id": "known-route",
      "when": "An exact, previously verified SSH alias or connection record exists for this device.",
      "steps": [
        {
          "id": "select",
          "actor": "agent",
          "action": "Load only the exact device record and its last verified route. Confirm the requested capability and operator authority; do not search every host or ask again for already verified details.",
          "proof": "The target identity, route, authority and last verification date are explicit."
        },
        {
          "id": "recheck",
          "actor": "agent",
          "action": "Use the selected existing alias with agentos setup probe ssh-vnc --host ALIAS --apply; add --ssh-only when only shell access is requested. This checks strict host-key policy and bounded authenticated SSH command execution, then optional RFB transport. Stop at the requested transport proof; do not launch a desktop viewer.",
          "proof": "Strict known-host checking and authenticated command execution succeed for the device identity already bound to the selected private route. A TCP or SSH banner alone is insufficient."
        }
      ]
    },
    {
      "id": "new-ssh-route",
      "when": "No verified route exists, or the prior route is stale or fails.",
      "steps": [
        {
          "id": "choose",
          "actor": "agent",
          "action": "Choose direct SSH when reachable; otherwise use an already trusted jump host. Use a reverse SSH forward only when the device cannot accept inbound access and an authorized broker is available. Explain the single chosen path and ask only for missing facts.",
          "proof": "The selected path has an exact target, verified broker if any, and no unnecessary new account or tunnel."
        },
        {
          "id": "prepare",
          "actor": "agent",
          "action": "Check the target SSH service and selected operator identity. If setup is needed, present the exact rights change and use a separately authorized platform flow; leave unavoidable OS approval to the person. For a reverse route, restrict forwarding on the broker and bind its listener to loopback only.",
          "proof": "The service and account have the intended scope, required approval is recorded, and the broker's effective listener is loopback-only."
        },
        {
          "id": "verify",
          "actor": "agent",
          "action": "Verify the target and broker host keys through a trusted source, then test listener reachability and a bounded authenticated SSH identity command using the approved path.",
          "proof": "Transport and authenticated device identity are recorded separately; the SSH command succeeds on the exact target."
        }
      ]
    },
    {
      "id": "desktop-over-ssh",
      "when": "Optional VNC transport verification is requested and SSH access to the exact device is verified; desktop viewer integration is deferred.",
      "steps": [
        {
          "id": "probe-transport",
          "actor": "agent",
          "action": "Follow docs/SSH_VNC_FIELD_GUIDE.md and run agentos setup probe ssh-vnc --host ALIAS --apply against an existing authorized endpoint. The bounded OpenSSH stdio channel closes after the RFB greeting and security offer. Do not enable Screen Sharing, launch a viewer or request VNC credentials.",
          "proof": "SSH authentication and an RFB response succeed; VNC authentication and desktop_frame_verified remain false."
        },
        {
          "id": "record-limit",
          "actor": "agent",
          "action": "Record only the observed SSH and RFB layers and cleanup. Defer Screen Sharing, other viewer integration and desktop acceptance to a separate stage.",
          "proof": "The result makes no claim of usable desktop access and creates no pending user login requirement."
        }
      ]
    }
  ],
  "sensitive_inputs": [
    "SSH private keys and passphrases",
    "SSH host key fingerprints and device addresses",
    "broker credentials and connection aliases",
    "device and user identity records"
  ],
  "recovery": [
    "If an old alias fails, revalidate the exact device and host key before changing the route; never suppress host-key warnings for speed.",
    "If a tunnel or banner works but SSH authentication fails, inspect the selected account and key without claiming device access.",
    "If SSH works but RFB transport fails, check the existing endpoint and selected channel independently. Do not start viewer setup as a recovery shortcut.",
    "Stop temporary forwards when finished. Remove only access created for this connection and record how the owner can revoke the key or broker authorization.",
    "ClearAllForwardings=yes suppresses explicit -L forwards while SSH can remain connected. The command-only probe uses yes; a requested VNC forward requires no.",
    "A missing listener in an unprivileged process-table query does not prove that a socket-activated service is unavailable; use a bounded connection to the exact endpoint."
  ],
  "maintenance": {
    "owner": "AgentOS community maintainers",
    "review_triggers": [
      "OpenSSH forwarding or host-key guidance changes",
      "Windows or macOS SSH setup changes",
      "RFB or VNC security guidance changes",
      "a verified route, device identity or credential is changed or revoked",
      "a tunnel succeeds while SSH authentication or RFB transport fails",
      "scheduled primary-source review"
    ]
  }
}
